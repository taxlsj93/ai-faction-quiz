# 🔍 ai-faction-quiz 코드 레벨 개선 진단서

> 작성일: 2026-05-26 · 작성: Claude Code (진단·제안 전용, 코드 무수정)
> 대상 커밋: `5b5c0c2` (2026-05-13) · 라이브: https://ai-faction-quiz.vercel.app
> 관점: 세무사가 사이드로 운영하는 **트래픽·수익형 콘텐츠 미디어**

---

## 0. 현황 요약 (코드 실측 기준)

| 항목 | 실측 결과 |
|------|----------|
| 제품 | 8문항 × 4지선다 AI 성향 퀴즈, KO=`index.html`(2,326줄), EN=`en.html`(1,120줄) |
| 결과 타입 | 4종 (Claude/GPT/Gemini/Grok), 타이 시 랜덤 |
| 백엔드 | `api/chat.js` — Vercel 서버리스, Claude Haiku 4.5 멀티턴 챗 (KO 전용) |
| 랜딩 | `/r/{faction}` 4종 (개별 OG 메타), `/privacy`, `/disclosure` |
| 분석 | GA4(`G-HPT1Y41HD8`) + Vercel Insights + Plausible(주석) · 이벤트 13종 |
| 광고 | AdSense Auto Ads (수동 `<ins>` 유닛 없음) + popunder망(주석 비활성) |
| 어필리에이트 | 2차 CTA 전부 writesonic/perplexity `?via=aifaction` (placeholder) |
| 뉴스레터 | **없음** (이메일 수집 입력란 0개) |

**실증 확인 (라이브 HTTP HEAD, 2026-05-26):**
- `/og-image.svg` → `Content-Type: image/svg+xml` ✅ 확인
- `/og/claude.svg` → `Content-Type: image/svg+xml` ✅ 확인
- `index.html` 전송량 ≈ 100KB (CSS/JS/데이터 전부 인라인 단일 파일)

---

## 🚨 1. 가장 치명적 — OG 이미지가 SVG (공유 미리보기 전면 깨짐)

**현재 상태**
- 모든 `og:image` / `twitter:image`가 `.svg` (`og-image.svg`, `og/{faction}.svg`).
- 라이브 응답 `Content-Type: image/svg+xml` 실측 확인.

**문제점 (이 제품의 존재 이유를 무력화)**
- **Facebook·X(트위터)·카카오톡·LinkedIn·Discord·Slack은 SVG를 OG 썸네일로 렌더링하지 않음.** 모두 PNG/JPG만 지원.
- 즉 친구에게 결과를 공유해도 **이미지 없는 휑한 링크**만 뜸. 퀴즈 바이럴의 핵심 연료(예쁜 결과 카드 미리보기)가 0.
- 아이러니하게 아키텍처는 이미 완벽함 — 공유 URL이 `/r/{faction}`(타입별 개별 OG)로 가고, 타입별 SVG도 따로 만들어 둠. **포맷만 PNG면 바로 개인화 미리보기가 작동.**

**개선안**
1. 기존 `og/*.svg` 4종 + 메인 `og-image.svg`를 **1200×630 PNG로 변환**해 `og/*.png`로 배포, 메타의 `og:image`/`twitter:image` 확장자만 교체.
   - 정적 PNG 4장이면 충분 (가장 단순·확실).
   - 또는 `@vercel/og`(Satori)로 런타임 동적 PNG 생성 — 점수·MBTI까지 카드에 박을 수 있어 확장성 ↑.
2. 배포 후 **반드시** 페이스북 [Sharing Debugger], X [Card Validator], 카카오 공유 디버거로 캐시 무효화 + 미리보기 확인.

**우선순위: 🔴 최상 (지금 당장)** · **난이도: 낮음** (정적 PNG) ~ 중간(@vercel/og)

---

## 2. 콘텐츠 / 퀴즈 품질

**현재 상태**
- 8문항, 각 문항 보기 A/B/C/D가 **거의 항상 claude/gpt/gemini/grok 순서로 3점 고정 배분**.
- 결과 4종, 매치율 = `min(99, round(최고점/24×100))`.
- KO 결과 화면엔 정적 분석 + 멀티턴 챗까지 있어 풍부함 (강점).

**문제점**
- **변별력·예측가능성**: A=Claude, B=GPT… 패턴이 고정이라 눈치 빠른 유저는 답을 "골라서" 원하는 타입을 만들 수 있음 → "신기하다" 마법이 약화. 보기 순서 무작위화 부재.
- **결과 쏠림 추정**: "신중·사색" 류(A) 보기가 사회적으로 바람직하게 들려 **Claude 타입 과대표집** 가능성. 실제 GA4 `quiz_complete`의 faction 분포로 검증 필요.
- **재미·바이럴 후킹**: 결과가 4종뿐 → "나도 너랑 같은 타입ㅋㅋ" 외에 변주가 적음. 희소성(레어 타입), 백분위("상위 7% Grok 타입"), 보조 타입 조합(예: Claude×Grok 하이브리드) 같은 **공유 욕구를 자극하는 장치** 부족.

**개선안**
- 보기 순서 셔플(점수는 보기에 귀속돼 있어 표시 순서만 섞으면 안전) → 예측가능성 제거.
- 결과에 **희소도/백분위** 표기. 실데이터 누적 전엔 점수 기반 의사-백분위로 시작.
- "보조 타입" 노출 강화(이미 `renderStaticAnalysis`에 10%↑ 보조타입 계산 로직 있음 → 결과 헤더로 승격).
- 문항 1~2개를 더 "재미·도발형"으로 (현재는 다소 진지/설명형).

**우선순위: 🟠 높음** · **난이도: 중간** (콘텐츠 + 약간의 JS)

---

## 3. 바이럴 / 공유

**현재 상태**
- 공유 수단: 트위터 인텐트, 카카오(`navigator.share`/클립보드), 인스타(캔버스로 1080×1080 JPG 생성·다운로드 — 잘 만듦), 링크복사.
- 공유 URL은 `/r/{faction}` (타입별 랜딩, 개별 OG 메타 보유).
- `?f={faction}` 딥링크 → "친구 결과 미리보기" 화면(showPreviewFull)도 구현돼 있음.

**문제점**
- 위 1번(SVG)으로 **모든 공유 미리보기가 무력화** → 이게 바이럴의 1차 병목.
- **`?f=` 미리보기 루프가 사실상 고아(orphan)**: 공유는 `/r/{faction}`(정적 페이지)로 가는데, 더 매력적인 "친구 결과 미리보기→나도 하기" 인터랙티브 루프(`?f=`)는 어디서도 공유되지 않음. 둘 중 하나로 통일 필요.
- "내 친구 진영은?" 류 **상호 비교 장치 없음** — 1:1 대결, 친구 초대 카운트, 커플/팀 궁합 같은 2차 확산 훅 부재.

**개선안**
- 1번 해결로 `/r/{faction}` 미리보기부터 살리기 (가장 빠른 ROI).
- `/r/{faction}` 페이지에 **결과 카드 이미지 + "친구는 무슨 타입?" 공유 버튼** 추가 → 받은 사람도 즉시 재공유.
- "내 타입 vs 친구 타입" 궁합/대결 모드(딥링크 2개 조합) — 2차 바이럴 계수↑.

**우선순위: 🟠 높음** (1번 직후) · **난이도: 중간**

---

## 4. 수익화

**현재 상태**
- 실수익원 = 어필리에이트뿐. 2차 CTA 4타입 전부 `writesonic.com/?via=aifaction` 또는 `perplexity.ai/?via=aifaction`.
- 결과 내 "추천 툴(allies)"은 Notion/Otter/Zapier 등 **비제휴(수익 0) 링크 다수 혼재**.
- AdSense는 Auto Ads만(수동 광고 유닛 `<ins>` 없음), 심사/트래픽 대기. popunder망은 주석 비활성.

**문제점**
- **`?via=aifaction`는 placeholder** — 실제 가입 트래킹 ID와 일치하는지 미검증. 불일치 시 전환돼도 **수익 0원으로 누락**. (STATUS/HANDOFF에도 미해결로 기재됨)
- **타입↔툴 적합도 미스매치**: "사색형 Claude 타입"에게 SEO 대량생산 툴 Writesonic을 미는 건 페르소나와 어긋남 → 전환율↓. 타입 성향에 맞는 툴 매핑이 필요.
- 결과 화면의 무료/비제휴 링크가 **클릭을 흡수**해 수익 링크로 갈 트래픽을 분산.
- 가정한 전환율 2%는 콜드 트래픽 기준 낙관적. 실측 전엔 보수적으로 볼 것.

**개선안**
- ① 실 가입 → 각 프로그램의 **진짜 트래킹 파라미터 확인·교체**(우선). 라이브 링크가 본인 대시보드에 클릭으로 잡히는지 1건 테스트.
- ② 타입별 어필리에이트 재매핑(예: 코딩 성향 GPT→Cursor/Replit류, 리서치 Gemini→Perplexity, 글쓰기 Claude→Notion AI 제휴 등) + **비제휴 링크는 줄이거나 제휴로 대체**.
- ③ AdSense 승인 후 결과 페이지 **하단 1개 수동 유닛**만(읽기 흐름 방해 최소) 배치.

**우선순위: 🟠 높음 (①은 즉시)** · **난이도: 낮음(①)~중간(②)**

---

## 5. 유입 / SEO

**현재 상태**
- robots/sitemap/canonical/hreflang/JSON-LD(Quiz) 기본기는 양호.
- 인덱싱 가능한 실질 콘텐츠 페이지 = 사실상 메인 2개 + 얇은 랜딩 4개.

**문제점**
- **EN URL 전략 충돌**: `index.html`의 hreflang(en)은 `/?lang=en`을 가리키는데, `sitemap.xml`은 `/en`(별도 `en.html`)을 등재. 영어 유저가 두 갈래 URL로 분산 + **중복 콘텐츠/canonical 혼선**.
- 퀴즈 본문(문항)이 JS 렌더라 **크롤러가 읽을 본문 텍스트가 빈약** → "Claude vs ChatGPT" 류 정보성 검색 유입 잡기 어려움.
- 키워드 메타는 과다 나열(요즘 랭킹 기여 거의 없음). 정작 **유입을 만들 블로그/비교 콘텐츠가 0** (Phase 2 미착수).

**개선안**
- EN 정본 URL 하나로 통일(`/en` 권장) + canonical/hreflang/sitemap 일치.
- `/r/{faction}` 랜딩에 타입 설명 **실텍스트 보강**(크롤 가능한 본문) + 내부링크.
- "어떤 AI 써야 할까 / Claude vs ChatGPT 차이" 비교 글 3~5편(정보성) → 퀴즈로 유도하는 콘텐츠 엔진.

**우선순위: 🟡 중간** · **난이도: 중간~높음(콘텐츠 분량)**

---

## 6. 분석 / 측정

**현재 상태**
- 이벤트 13종 풍부(quiz_start/complete, cta_*, affiliate_card_click, chat_* 등). 헬퍼 `trackEvent`로 GA4+Plausible 동시 전송 설계.

**문제점**
- 이벤트는 많은데 **퍼널/전환 목표(conversion)로 안 엮임** → "데이터는 쌓이는데 의사결정엔 안 쓰임".
- 핵심 KPI 미설정: 시작→완료율, 완료→공유율, 완료→2차CTA(수익) CTR, 챗 오픈율, 타입 분포.
- Search Console 미등록(STATUS 기재) → 검색 노출/쿼리 데이터 부재.

**개선안**
- GA4에서 `quiz_complete`·`cta_secondary_click`·`chat_opened`를 **주요 이벤트(전환)로 표시** + 탐색>유입경로(funnel) 1개 구성.
- 한 화면 대시보드: 완료율 / 공유율 / 수익CTA CTR / 타입 분포 — 매주 확인.
- Search Console 등록 + sitemap 제출(모바일로 5분).

**우선순위: 🟡 중간** · **난이도: 낮음** (대부분 GA4 콘솔 설정)

---

## 7. 재방문 / 확장

**현재 상태**
- 일회성 퀴즈 1종. 재방문 동기·구독 자산 없음.

**문제점**
- 퀴즈는 본질적으로 1회성 → 트래픽이 "왔다 사라짐". **붙잡을 자산(이메일)·돌아올 이유(신규 콘텐츠)** 부재.
- 뉴스레터 입력란 0개(실측). Phase 3 게임(phoney-war) 보류.

**개선안**
- **결과 화면에 가벼운 이메일 수집**(ConvertKit 무료) — "새 AI 퀴즈/뉴스 받기". 일회성 트래픽을 구독 자산으로 전환(이 사업에서 가장 중요한 장기 레버).
- **시즌제/신규 퀴즈**: "2026 AI 툴 궁합", "내 업무에 맞는 AI", "AI 빌런 테스트" 등 — 같은 엔진 재활용, 재방문 유도.
- 집계 통계 공개("지금까지 Claude 타입 32%") → FOMO·재방문.

**우선순위: 🟡 중간 (뉴스레터는 높음에 가까움)** · **난이도: 중간**

---

## 8. 기술 / UX / 성능 / 코드구조

**현재 상태 & 관찰**
- **단일 100KB 인라인 HTML** (CSS+JS+데이터 전부). SPA 첫 페인트엔 유리하나 **유지보수성 낮음**.
- **KO/EN 코드 드리프트(중요)**: `en.html`엔 멀티턴 챗·정적 AI 분석·`/api/chat` 호출이 **전부 빠짐**(실측). 차별화 기능이 영어권엔 미제공 → 분리된 두 파일이 따로 노는 전형적 증상.
- 뷰포트 `maximum-scale=1.0` → **핀치 줌 차단(접근성/WCAG 위반)**.
- 로딩 화면이 **인위적 ~2.1초 지연**(700ms×3 고정) → 모바일 이탈 유발 가능. "분석 중" 연출 대비 마찰 큼.
- 인스타 공유 캔버스가 **Arial 하드코딩** → 한글 폰트 폴백에 따라 자형이 깨질 수 있음(기기별 확인 필요).
- `.github/` 워크플로가 **untracked(미커밋)** → CLAUDE.md가 말하는 "Claude GitHub Action 자동화"는 **실제로 비활성**.
- 작업트리 미커밋: `CLAUDE.md`(M), `deploy/`·`audit-report.html`·`session-report.html`(미정리).

**개선안**
- KO/EN을 **공통 로직 1벌 + 언어 리소스 분리** 또는 최소한 EN에도 챗/분석 이식(드리프트 해소).
- `maximum-scale` 제거(줌 허용).
- 로딩 지연 단축(≤1초) 또는 실제 호출과 동기화.
- 캔버스 폰트에 한글 웹폰트/시스템 한글 스택 명시.
- `.github/` 커밋해 액션 실제 활성화(원하면).

**우선순위: 🟡 중간 (EN 패리티·줌은 높음)** · **난이도: 중간**

---

## 9. 우선순위 종합표

| # | 개선 | 축 | 우선순위 | 난이도 | 한 줄 효과 |
|---|------|-----|:--:|:--:|------|
| 1 | **OG 이미지 SVG→PNG** | 바이럴 | 🔴 최상 | 낮음 | 공유 미리보기 부활 = 바이럴 엔진 ON |
| 2 | **어필리에이트 실 트래킹 ID 검증·교체** | 수익 | 🔴 최상 | 낮음 | 누락 수익 0원 방지 |
| 3 | 공유 루프 통일(`/r/` ↔ `?f=`) + 재공유 버튼 | 바이럴 | 🟠 높음 | 중간 | 2차 확산 계수↑ |
| 4 | 결과 차별화(희소도·백분위·보조타입 승격) | 콘텐츠 | 🟠 높음 | 중간 | 공유 욕구·재미↑ |
| 5 | 타입↔툴 적합도 재매핑 + 비제휴 링크 정리 | 수익 | 🟠 높음 | 중간 | 전환율↑ |
| 6 | 뉴스레터(ConvertKit) 결과화면 수집 | 재방문 | 🟠 높음 | 중간 | 일회성→구독 자산 |
| 7 | EN 기능 패리티(챗/분석) + EN URL 통일 | 기술/SEO | 🟡 중간 | 중간 | 영어권 차별화·중복콘텐츠 해소 |
| 8 | GA4 전환목표·퍼널 + Search Console | 측정 | 🟡 중간 | 낮음 | 데이터→의사결정 |
| 9 | 보기순서 셔플 + 결과쏠림 검증 | 콘텐츠 | 🟡 중간 | 낮음 | 변별력·예측불가성 |
| 10 | 줌 허용·로딩단축·코드구조 | UX | 🟡 중간 | 중간 | 접근성·이탈↓ |
| 11 | 비교/블로그 콘텐츠 엔진 | SEO | 🟢 낮음 | 높음 | 검색 유입(장기) |

---

## 🎯 지금 당장 하면 좋을 TOP 5

1. **OG 이미지를 PNG로 교체** — 이거 하나가 공유 미리보기 전체를 살림. 다른 모든 바이럴 개선의 전제. (난이도 낮음, 효과 최대)
2. **어필리에이트 `?via=aifaction`가 진짜 내 ID인지 라이브 1건 클릭 테스트** — 전환돼도 수익 누락이면 다 헛수고.
3. **GA4에서 `quiz_complete`·`cta_secondary_click`·`chat_opened`를 전환으로 표시 + Search Console 등록** — 모바일로 가능, 무엇을 고쳐야 할지가 보임.
4. **결과 화면에 이메일 수집(ConvertKit 무료) 한 줄 추가** — 일회성 트래픽을 돌아올 구독자로. 장기 레버 1순위.
5. **공유 루프 통일 + `/r/{faction}`에 "친구는 무슨 타입?" 재공유 버튼** — OG가 살아난 직후 2차 확산을 받아낼 그릇.

> ⚠️ 본 문서는 진단·제안 전용. 코드는 일절 수정하지 않았으며, 커밋도 하지 않았습니다.
