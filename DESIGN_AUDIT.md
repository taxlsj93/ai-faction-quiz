# DESIGN_AUDIT.md — ai-faction-quiz UX/UI 진단·개선안

> 작성일: 2026-05-27  
> 작성: Designer (oh-my-claudecode)  
> 대상: `index.html` (2,326줄) + `r/{faction}.html` 4종  
> 관점: 모바일 한 손 조작 · 트래픽·수익형 콘텐츠 미디어  
> 코드 무수정 — 진단·제안 전용

---

## 1. 현재 상태 진단

### 1-1. 메인/인트로 (`#screen-intro`)

**강점:** `clamp(28px, 8vw, 42px)` 헤드라인이 반응형으로 잘 동작. 타입 4종을 2×2 그리드로 미리 보여줘 기대감 유발. "8문항 · 2분 소요 · 무료" 메타 정보가 진입 마찰을 낮춤.

**약점:**
- 인트로 배지(`AI 성향 테스트`)가 11px uppercase로 너무 작고 `--muted` 색상이라 첫 시선을 잡지 못함. 기능보다 장식에 가까운 상태.
- `btn-start`의 `linear-gradient(135deg, #6C3FA8, #3B5FD4)`는 배경(`--bg: #0A0A0F`)과 명도 대비는 충분하나, 브랜드 4색 중 어떤 타입도 아닌 중립 보라·파 그라디언트라 브랜드 정체성이 희박. 첫 진입 순간 가장 눈에 띄어야 할 버튼이 "어느 타입의 컬러도 아님".
- `current-ai-section`(현재 쓰는 AI 선택) 레이블이 `font-size:12px` + 올캡 + `--muted` — 선택 유도 문구 치고 너무 약함. 해당 선택이 크로스 얼라이 로직(`cross-allies-section`)과 연결된다는 사실을 유저에게 전혀 알리지 않아 기능 가치를 낭비.
- 타입 칩(`.faction-chip`)의 `border-color`가 각 타입 컬러의 극도로 어두운 버전(`#3A2448`, `#0D3D2E` 등)이라 모바일 환경에서 타입 구분 시각화가 약함.

### 1-2. 퀴즈 진행 (`#screen-quiz`)

**강점:** 프로그레스 바(`height:4px`)가 심플하고 방해 없음. `pointerEvents:'none'` 즉시 적용으로 중복 선택 방지. 360ms 딜레이 후 다음 질문 자동 이동 — 흐름이 자연스러움.

**약점:**
- **선택 후 피드백이 없음.** 선택하면 `selected` 클래스(보라 배경)만 붙고 360ms 후 그냥 전환. "내가 무언가를 선택했다"는 만족감을 주는 시각적 펄스나 체크 아이콘이 전혀 없음. 터치 기기에서 빠른 응답감 부재.
- Q 번호 표시(`q-num`)가 `font-size:12px` uppercase `--muted`로 너무 약함. "Q3 / 8" 정보가 중요한 진행감 신호인데 시각 위계상 거의 안 보임.
- 프로그레스 바가 문항 시작 시점 퍼센트(0→87.5%) 기준으로 채워져, Q8 선택 후 로딩으로 넘어가기 직전까지 바가 100%에 도달하지 않음 — 완료 시그널 부재. (`pct = (currentQ) / QUESTIONS.length * 100` 라인 1724)
- 옵션 버튼 높이가 `padding:16px 18px`으로 최소 터치 타겟(44px)은 만족하나, 긴 텍스트 옵션에서 줄바꿈 시 터치 영역 예측이 어려워짐.

### 1-3. 로딩 화면 (`#screen-loading`, 라인 952-967)

**강점:** 3단계 메시지 교체("성향 분석 중" → "데이터 대조 중" → "타입 확정 중")가 분석 중이라는 서사를 만들어 주관적 대기 시간을 줄임. `loading-ad` 슬롯이 `animation: fadeIn 0.5s ease 1.2s forwards`로 지연 노출 — 콘텐츠 먼저, 광고 나중 순서 올바름.

**약점:**
- 총 대기 시간 700ms × 3 = **2,100ms 고정 지연**. 실제 계산은 즉시 끝나는데 인위적으로 2.1초 묶음. 모바일 4G/LTE 유저에게 불필요한 이탈 마찰. 특히 KO → 결과 화면 진입 전 AI 분석 API 호출도 없이 이 지연만 있음.
- 로딩 링(`loading-ring`)이 단색 보라(`border-top-color: #6C3FA8`) 스핀으로 단순. 타입 결정이 가까워질수록 긴장감을 높이는 타입 컬러 점진 전환 같은 연출 없음.
- 광고 슬롯이 로딩 화면에 뜨지만, 유저가 이 화면을 2초만 보기 때문에 실제 광고 노출·클릭 기회가 매우 짧음. 광고 노출 시간과 결과 기대감 사이의 균형 설계가 필요.

### 1-4. 결과 화면 (`#screen-result`)

**강점:** 타입별 `faction-*` 클래스로 CTA 버튼·아이콘 링·이름 색상이 자동 교체 — 일관된 타입 테마. `growBar` 애니메이션이 특성 바 진입 시 보상감 제공. AI 분석 섹션 + 챗 진입 CTA는 경쟁 퀴즈 서비스에 없는 차별화 자산.

**약점:**
- **결과 화면 정보 밀도가 과도함.** 타입 헤더 → AI 분석 → 특성 바 → 통계 카드 → 매치업 카드 → 점수 분포 → 얼라이 툴 → CTA 1차 → CTA 2차 → 공유 섹션 → 면책 → 다시 테스트까지 스크롤 깊이가 매우 길다. 결과 확인 직후 핵심 CTA(공유)에 도달하기 전에 에너지 소진.
- `result-match`(`타입 적합도 72%` 등)가 `font-size:13px; opacity:0.8`로 눈에 잘 안 띔. 공유 욕구를 자극하는 가장 강력한 숫자인데 위계가 타입명(`font-size: clamp(30px,9vw,44px)`)과 너무 차이남.
- 보조 타입(`secondaryLine`)이 AI 분석 섹션 내부 말미 텍스트로 삽입됨(라인 1940-1942). **"보조 타입: Grok 타입 (28%)"** 같은 정보가 결과 헤더 바로 아래에 배지 형태로 올라와야 공유 욕구↑인데, 분석 텍스트 안에 묻혀 있어 거의 보이지 않음 (C3 작업과 직결).
- 2차 CTA(`btn-cta-secondary`)의 스타일이 `background:#1E1E2E; border:1.5px solid #333355; font-size:14px`로 배경에 묻히는 수준. 1차 CTA(타입 컬러 그라디언트)와 시각 위계 차이를 의도한 것은 맞으나, 너무 눈에 안 띄어 수익 전환율 저해.
- `ally-card` 링크들 중 비제휴(`Notion`, `Zapier`, `Canva`, `Otter.ai`, `Midjourney`, `ElevenLabs`)가 제휴 링크와 시각적으로 동일하게 표시됨 — 클릭 희석.

### 1-5. 공유 흐름

**강점:** X·카카오·인스타·링크복사 4종. 인스타용 1080×1080 캔버스 카드 생성이 경쟁 서비스 대비 강점. 타입별 훅 카피(`SHARE_HOOKS_KO`)가 구어체·자연스러움.

**약점:**
- 공유 버튼 그리드(`share-grid: 1fr 1fr`)가 공유 섹션 최하단에 위치 — 결과 화면의 긴 스크롤 끝에 존재. 모바일에서 공유까지 도달하는 스크롤 거리가 과도.
- 카카오 버튼이 `background: #FEE500; color: #191919`로 카카오 브랜드 색상을 정확히 사용했으나, 나머지 버튼(X·인스타·링크복사)은 모두 다크 카드 스타일이라 시각적 혼재감.
- 인스타 캔버스에 Arial 폰트 하드코딩(`ctx.font = '500 22px Arial'` 라인 2257, `ctx.font = 'bold 80px Arial'` 라인 2285). 한글이 폴백 폰트로 렌더링되면 기기별 자형 편차 발생 가능.
- 공유 URL이 `/r/{faction}`으로 정확히 가지만, `/r/{faction}` 랜딩에서 재공유 버튼이 없음 (C4 작업 예정). 공유 받은 사람이 재확산할 루프가 단절.

### 1-6. 챗 인터페이스 (`openChat()`, 라인 1982-)

**강점:** 5회 제한(`CHAT_LIMIT`) + 남은 횟수 표시로 희소성 부여. 타입별 스타터 칩이 진입 마찰을 낮춤. 스트리밍 없이 응답 완성 후 표시 — 단순하지만 안정적.

**약점:**
- 챗 진입 버튼(`btn-open-chat`)이 AI 분석 섹션 내부 하단에 숨어 있어 **결과 화면의 가장 강력한 체류·재방문 훅인데 노출이 취약**. 분석 텍스트를 다 읽어야 보임.
- 입력창(`ai-chat-input`)이 `padding:12px 14px; border-radius:11px`로 모바일에서 터치 영역이 적절하나, 전송 버튼(`ai-chat-send: padding:12px 18px`)이 항상 "전송" 텍스트 고정 — 아이콘(↑ 화살표) 없이 텍스트만이라 버튼 영역 인식이 약함.
- 챗 메시지 영역 `max-height:380px; overflow-y:auto`가 모바일에서 화면 높이를 초과하면 이중 스크롤(페이지 스크롤 + 챗 스크롤)이 발생할 수 있음.
- 한도 도달 시 `'이번 세션 질문 한도에 도달했어요. 페이지를 새로고침하면 다시 사용할 수 있어요.'` 문구가 `ai-chat-meta`(11px, --muted)로 표시되어 거의 안 보임. 뉴스레터 수집 CTA로 전환할 수 있는 기회 지점.

### 1-7. 뉴스레터 폼 (C6 예정)

현재 없음. 위치·카피·시각 무게 설계 필요 → 섹션 3 [#4] 참조.

### 1-8. `/r/{faction}` 랜딩 4종

**강점:** 타입 컬러 상단 4px 띠(`card-top`)로 즉각 브랜드 식별. 타입명·짧은 설명·CTA 버튼의 3단 구조가 깔끔. `other-types` 링크로 타 타입 탐색 유도.

**약점:**
- **재공유 버튼이 없음.** 랜딩에 도착한 친구가 "나도 해볼게" CTA만 있고, "나도 결과 공유하기" 루프가 없음. C4 작업의 핵심 갭.
- `card` 패딩이 `48px 40px`으로 데스크톱 기준 — 모바일(375px)에서 사이드 여백이 16px밖에 안 남아 숨막히는 느낌. 내부 콘텐츠가 화면을 꽉 채움.
- 타입 설명이 짧은 태그라인 2줄뿐 (`long-tag`). 크롤러가 읽을 텍스트 콘텐츠가 빈약 — SEO 실텍스트 보강 여지.
- 타입 이모지가 🟣🟢🔵🔴 컬러 서클만 사용. 직접 제작한 캐릭터 아이콘이 없어 개성이 약함.

---

## 2. 디자인 시스템 정합성

### 색 토큰

| 토큰 | 값 | 사용 일관성 |
|------|-----|-----------|
| `--claude` | `#C17CE4` | ✅ 타입명·바·아이콘 링 일관 |
| `--gpt` | `#10A37F` | ✅ |
| `--gemini` | `#4285F4` | ✅ |
| `--grok` | `#E7425D` | ✅ |
| `--bg` | `#0A0A0F` | ✅ |
| `--card` | `#13131A` | ✅ |
| `--border` | `#222230` | ✅ |
| `--text` | `#F0F0F8` | ✅ |
| `--muted` | `#8888AA` | ⚠️ 과용 — 중요한 정보(Q번호, 진행율 레이블, 챗 한도 메시지)까지 동일 색상으로 위계 붕괴 |

`btn-start`·`progress-fill`·`loading-ring`의 고정 `#6C3FA8 / #3B5FD4` 보라-파 그라디언트가 CSS 변수로 추상화되지 않아 타입 컬러 시스템과 독립적으로 존재. 일관성 유지 부담.

### 타이포그래피

| 요소 | 현재 값 | 평가 |
|------|--------|------|
| body font | `-apple-system, 'Segoe UI', sans-serif` | 시스템 폰트라 개성 없음. 한글에서는 -apple-system → Apple SD Gothic Neo, Segoe UI → 기본 굴림계 폴백 |
| 헤드라인 | `clamp(28px,8vw,42px)` fw800 | 크기는 적절, 폰트 자체가 시스템이라 인상이 약함 |
| 본문 | 15-16px lh1.4-1.6 | 가독성 양호 |
| 레이블류 | 11-13px uppercase letter-spacing | 과용 — 동일 스타일이 배지·통계 제목·매치업 제목 등에 반복되어 위계가 평탄 |

### 간격·라운드·섀도우

- 간격: `gap:8-14px`, `margin-bottom:8-28px`로 대체로 일관. 카드 간격 20px 표준화.
- 라운드: 카드 16px, 버튼 12-14px, 칩 12-20px — 혼재되어 있으나 허용 범위.
- 섀도우: **전혀 없음.** 다크 배경이므로 섀도우보다 `border + background` 레이어링으로 깊이 표현 — 현재 방식은 올바름.

### 트랜지션

- 화면 전환: `fadeIn 0.4s ease` — 충분.
- 버튼 interactive: `:active { transform: scale(0.98-0.99) }` — 모바일 탭 피드백 최소 만족.
- 선택 후 옵션 → 다음 화면: **트랜지션 없이 갑자기 교체됨** (renderQuestion() 직접 DOM 교체). 모바일에서 깜빡임처럼 느껴질 수 있음.
- `growBar` 애니메이션: `0.8s ease forwards` — 결과 화면 도착 시 보상감 제공, 적절.

---

## 3. 핵심 개선 포인트 (8개)

### [#1] 퀴즈 선택 순간 — 즉각 시각 피드백 추가

- **현재:** 옵션 선택 시 `selected` 클래스(보라 배경)만 붙고, 360ms 후 화면 교체. 선택된 버튼에 애니메이션 없음. (라인 1740-1757 `selectOption`)
- **제안:** `selected` 클래스 적용과 동시에 버튼에 단기 `scale(0.97) → scale(1.02) → scale(1)` 스프링 애니메이션 + 좌측 `opt-label`(A/B/C/D)을 체크마크(✓)로 교체. CSS로만 구현 가능: `.option-btn.selected { animation: selectPop 0.25s ease; }` + `@keyframes selectPop { 0%{transform:scale(1)} 40%{transform:scale(0.97)} 70%{transform:scale(1.02)} 100%{transform:scale(1)} }`. 선택 순간의 촉각적 만족감을 시각으로 보상.
- **우선순위:** 높음
- **구현 복잡도:** 낮음 (CSS 추가만)
- **예상 효과:** 퀴즈 완료율 소폭 상승 (선택 후 확신감 → 이탈 감소)
- **C? 매핑:** 독립 — 즉시 적용 가능

---

### [#2] 결과 화면 — 보조 타입 배지를 헤더로 승격

- **현재:** `secondaryLine`이 `renderStaticAnalysis()` 내부에서 AI 분석 텍스트 말미에 `**보조 타입: Grok 타입 (28%)**` 형태로 삽입됨. 분석 섹션 전체를 읽어야 보임. (라인 1931-1943)
- **제안:** 결과 헤더 영역(`result-header`)에서 `result-match`(타입 적합도 %) 바로 아래에 보조 타입 배지 추가. 구조 예시: `<div id="result-secondary-badge" style="display:none">` → `"+ Grok 기질 28%"` 형태로 타입 컬러 소형 배지 표시. 기존 `renderStaticAnalysis` 호출 시 배지도 함께 활성화. 보조 타입을 **공유 텍스트에도 포함** ("나는 Claude 타입 (+ Grok 기질 28%)"). 공유 욕구 및 희소감 대폭 상승.
- **우선순위:** 높음
- **구현 복잡도:** 낮음 (DOM 요소 1개 추가 + renderStaticAnalysis 수정)
- **예상 효과:** `share_*` 이벤트 증가 예상. GA4 `quiz_complete` 시 `secondary_type` 파라미터 추가로 분포 측정 가능.
- **C? 매핑:** C3 (결과 차별화)와 직결 — C3 구현 시 함께 처리

---

### [#3] 로딩 화면 — 지연 단축 + 타입 힌트 긴장감

- **현재:** 700ms × 3 = 2,100ms 고정 지연. 3번째 메시지("타입 확정 중...") 표시 후 700ms 더 대기 후 결과 화면 이동. 총 2.1초 순수 대기. (라인 1760-1770 `showLoading`)
- **제안 A (빠른 적용):** 인터벌을 `700ms → 500ms`로 단축 (총 1.5초). 체감 대기 28% 감소. 광고 노출 충분 여부는 `fadeIn 0.5s ease 1.2s`로 1.2초 후 등장하므로 500ms×3=1.5초 기준 약 0.3초 노출 시간 남음 — 허용 범위.
- **제안 B (추가 효과):** 3번째 메시지 전환 시 결과 타입 컬러를 `.loading-ring`에 적용 (`border-top-color`를 결과 faction 컬러로 변경). 유저가 타입을 아직 모르는 상태에서 컬러 힌트가 등장 — 긴장감 상승. `scores` 계산은 이미 완료되어 있으므로 `showLoading()` 진입 시점에 결과 타입을 미리 계산 후 3번째 단계에서 `document.querySelector('.loading-ring').style.borderTopColor = FACTIONS[resultFaction].color`로 적용.
- **우선순위:** 중간
- **구현 복잡도:** 낮음
- **예상 효과:** 모바일 이탈률 소폭 감소
- **C? 매핑:** 독립

---

### [#4] 뉴스레터 폼 (C6) — 위치·카피·시각 무게 설계

- **현재:** 없음. C6에서 ConvertKit 임베드 예정.
- **제안 위치:** 챗 한도 도달 직후(`updateChatMeta()` 호출 시점) + 결과 화면 얼라이 섹션 아래(`allies-section` 이후, CTA 버튼 이전). 두 진입점 중 **챗 한도 도달 시점**이 최적 — 유저가 이미 깊이 인게이지된 상태에서 자연스럽게 "더 받고 싶다" 욕구 포착.
- **카피 제안:**
  - 헤드라인: `내 AI 타입에 맞는 툴 · 뉴스 받기 (월 2회)`
  - 서브: `Claude 타입 유저 3,200명이 이미 구독 중`  (소셜 프루프 — 숫자는 런치 후 실수치로 교체)
  - 입력 플레이스홀더: `이메일 주소` + 버튼: `무료 구독하기`
  - 면책: `스팸 없음 · 언제든 해지 가능`
- **시각 무게:** 얼라이 카드와 동일한 `.stats-card` 스타일(background: var(--card); border: 1px solid var(--border); border-radius:16px; padding:20px) 활용. 타입 컬러 좌측 3px 세로 border-left 추가로 타입 연관성 표시. 별도 CSS 신규 컴포넌트 최소화.
- **우선순위:** 높음
- **구현 복잡도:** 중간 (ConvertKit 임베드 + 챗 한도 콜백 연결)
- **예상 효과:** 일회성 트래픽의 5-10%를 이메일 구독자로 전환 목표. 장기 재방문 자산.
- **C? 매핑:** C6 직결

---

### [#5] `/r/{faction}` 랜딩 — 재공유 CTA + 모바일 패딩

- **현재:** `card` 패딩 `48px 40px`. "나도 테스트 해보기 →" 버튼 1개만 존재. 재공유 없음. (r/claude.html 라인 42-70)
- **제안:**
  1. **모바일 패딩 수정:** `@media (max-width:480px) { .card { padding: 32px 20px; } }` — 375px 화면에서 카드 내부 여백 확보.
  2. **재공유 버튼 추가:** "나도 테스트 해보기 →" 버튼 아래에 `<button onclick="shareResult()">📣 친구에게 공유하기</button>` 추가. 공유 시 해당 타입 `/r/{faction}` URL + 타입별 훅 카피로 X 인텐트·클립보드 복사.
  3. **타입 키워드 텍스트 보강:** `long-tag` 아래에 특성 3줄 텍스트(`<p class="traits-text">`) 추가 — 크롤러 인덱싱 + 유저 공유 욕구 상승.
- **우선순위:** 높음
- **구현 복잡도:** 낮음 (각 r/*.html에 동일 패턴 추가)
- **예상 효과:** `/r/{faction}` → 퀴즈 전환율 + 2차 공유 계수 상승. GA4 `share_*` 이벤트 추가로 측정.
- **C? 매핑:** C4 (재공유 CTA) 직결

---

### [#6] 결과 화면 — 공유 섹션 상단 부상

- **현재:** 공유 버튼 그리드가 결과 화면의 최하단, CTA 블록 이후에 위치. 스크롤 순서: 타입 헤더 → AI 분석 → 특성 바 → 통계 → 매치업 → 점수 분포 → 얼라이 → CTA 1차+2차 → **공유 섹션** → 면책 → 다시 테스트.
- **제안:** 공유 섹션을 **타입 헤더 바로 아래**로 이동. 즉, 결과 확인 → 즉시 공유 루프 형성. 구체적 순서: 타입 헤더(`result-header`) → 공유 섹션(`share-section`) → AI 분석 → 특성 바 → 통계/매치업 → CTA. 이 구조에서 분석·통계는 "공유 후 더 읽는" 딥다이브 영역이 됨. 공유 레이블 카피를 `내 타입을 알려라 — 친구를 끌어들여라`에서 `[타입명] 나왔다! 친구에게 도전장 보내기`로 교체 (타입별 동적 생성).
- **우선순위:** 높음
- **구현 복잡도:** 낮음 (DOM 순서 변경만)
- **예상 효과:** `share_*` 이벤트 증가 예상. GA4 `quiz_complete → share_*` 전환율 측정으로 검증.
- **C? 매핑:** C4 (공유 카피 통일) 연동

---

### [#7] 챗 진입 버튼 — 노출 강화

- **현재:** `btn-open-chat`이 `ai-analysis-section` 내부 말미에 위치. `display:none` 상태로 시작 후 `renderStaticAnalysis()` 완료 시 `display:block`으로 전환. 버튼 텍스트 `💬 내 AI에게 직접 질문하기` + 11px 힌트 텍스트. (라인 986-989)
- **제안:**
  1. AI 분석 섹션 헤더(`ai-analysis-header`) 우측에 챗 진입 배지("💬 질문하기") 인라인 배치 — 섹션 들어오면 즉시 인지.
  2. `btn-open-chat` 배경을 타입 컬러 10% 알파 → 20% 알파로 상향. 현재 `rgba(193,124,228,0.15)` → `rgba(193,124,228,0.25)`.
  3. 챗 한도 도달 후 `ai-chat-meta` 문구를 타입 컬러 텍스트로 강조 + 뉴스레터 폼 CTA 인라인 연결.
- **우선순위:** 중간
- **구현 복잡도:** 낮음
- **예상 효과:** `chat_opened` 이벤트 증가. 체류 시간 상승.
- **C? 매핑:** 독립 — C6 뉴스레터 연결 시 시너지

---

### [#8] 인트로 — 현재 AI 선택 섹션 가치 전달

- **현재:** `current-ai-section` 레이블이 `지금 쓰고 있는 AI가 있나요? (선택)` 12px uppercase --muted. 선택의 의미를 알 수 없음. (라인 917-925)
- **제안:** 레이블을 `지금 쓰는 AI 선택하면 → 내 상황에 맞는 추가 툴 추천`으로 교체하고 font-size를 13px, color를 `var(--text)`의 70% 알파로 상향. `current-ai-grid`의 선택된 버튼에 타입 컬러 `box-shadow: 0 0 0 2px {color}40` glow 효과 추가. 이 선택이 `cross-allies-section`과 연결된다는 것을 명시적으로 예고해 선택률↑ → 크로스 얼라이 클릭률↑ → 제휴 수익 상승.
- **우선순위:** 중간
- **구현 복잡도:** 낮음
- **예상 효과:** `currentAI` 선택률 상승 → `cross-allies-section` 노출 증가 → `affiliate_card_click` 이벤트 상승.
- **C? 매핑:** 독립

---

## 4. 디자인 토큰 권고

| 토큰명 | 현재 값 | 제안 값 | 이유 |
|--------|--------|--------|------|
| `--accent-neutral` | 없음 (하드코딩 `#6C3FA8`) | `#6C3FA8` 변수화 | btn-start·progress-fill·loading-ring·ad-slot-cta 등 6곳에 분산 하드코딩. 한 곳 변경으로 통일 가능하게. |
| `--border-interactive` | 없음 | `#333355` | 인터랙티브 요소 hover/focus 시 border 색상. 현재 `#444466`·`#555575` 등 혼용. |
| `--text-secondary` | 없음 (--muted 남용) | `#AAAACC` | `--muted(#8888AA)`보다 밝은 중간 단계. Q번호·진행 레이블·보조 정보에 사용해 3단계 위계 확보. |
| `--radius-card` | 없음 (14-16px 혼용) | `16px` | 카드류(`stats-card`, `matchup`, `score-dist`, `ai-analysis-section`)의 border-radius가 14·16px 혼재. 16px로 통일. |
| `--transition-fast` | 없음 | `0.15s ease` | 버튼 hover/border-color 트랜지션이 `0.15s`·`0.2s`·`0.2s` 혼재. 변수화 권장. |

---

## 5. 모바일 한 손 조작 우선 권고 5선

**기준:** 오른손 엄지 기준, 375px 화면, 스크롤 최소화, 하단 1/3 액션 집중.

1. **공유 버튼 상단 이동 ([#6]):** 현재 결과 화면 최하단에 위치한 공유 그리드를 타입 헤더 바로 아래로. 공유는 퀴즈 완료 직후 감정이 가장 고조된 순간에 발생해야 함. 스크롤 없이 도달 가능하도록.

2. **결과 화면 고정 공유 바 (Floating CTA):** 결과 화면(`#screen-result`) 진입 시 화면 하단에 고정 바 노출 — 좌측: `[타입 이모지] Claude 타입 나왔다!`, 우측: `공유하기` 버튼. 스크롤 위치 무관하게 공유 접근 가능. 스크롤 일정 거리 후 자동 노출, 탭으로 닫기 가능. `position:fixed; bottom:0; left:0; right:0; z-index:100; background: var(--bg); border-top: 1px solid var(--border); padding:12px 16px;`

3. **옵션 버튼 최소 높이 보장:** 현재 `padding:16px 18px`로 최소 44px 충족이지만 텍스트 2줄 이상 시 터치 영역이 예측 불가능하게 커짐. 긴 옵션 텍스트에 `min-height:52px` 명시적 설정으로 일관된 리듬 확보.

4. **인트로 CTA 버튼 엄지 구역 최적화:** `btn-start`는 현재 인트로 화면 하단에 위치하나, 그 위의 `current-ai-section`(4버튼 그리드)과 `factions-preview`(2×2 그리드)가 긴 스크롤 없이 한 화면에 보이는지 확인 필요. 375px × 812px(iPhone 기준) 에서 전체 인트로가 스크롤 없이 보이도록 `factions-preview margin-bottom: 24px → 20px`, `faction-chip padding: 14px → 12px`로 소폭 압축.

5. **챗 입력창 — 키보드 올라올 때 스크롤 처리:** 모바일에서 `ai-chat-input` 포커스 시 가상 키보드가 올라오면 입력창이 키보드에 가릴 수 있음. `scrollIntoView({ behavior:'smooth', block:'end' })` 호출을 input `focus` 이벤트에 추가. 현재 `sendChat()` 말미에 `input.focus()` 호출은 있으나 스크롤 처리는 없음 (라인 2109).

---

## 6. 데스크톱 무손상 체크 항목

구현 시 아래 항목은 데스크톱(`max-width:480px` 컨테이너 기준) 레이아웃·동작에 영향 없어야 함.

| 항목 | 검토 내용 |
|------|---------|
| Floating CTA 바 ([#5 권고 2]) | `max-width:480px; margin:0 auto`로 컨테이너 너비 제한. 데스크톱 전체 너비 가득 채우지 않도록. |
| 공유 섹션 DOM 순서 변경 ([#6]) | `#screen-result` 내부 순서 변경이므로 데스크톱 동일 컨테이너. 컬럼 레이아웃이 없으므로 영향 없음. |
| `r/*.html` 모바일 패딩 수정 ([#5]) | `@media (max-width:480px)` 조건부 적용으로 데스크톱 패딩 `48px 40px` 유지. |
| AI 분석 섹션 챗 배지 ([#7]) | 인라인 flexbox 요소 추가. `ai-analysis-header: display:flex`에 `justify-content:space-between` 추가 시 데스크톱·모바일 동일 동작. |
| GA4/Plausible 이벤트 추가 | 기존 `window.trackEvent` 헬퍼 재사용. 신규 이벤트 파라미터는 기존 13종과 충돌 없이 추가. |
| AdSense Auto Ads | DOM 순서 변경 및 CSS 추가가 AdSense 삽입 위치에 영향 없음 (Auto Ads가 자체 위치 결정). `loading-ad` 슬롯 HTML 무변경 전제. |
| Plausible 주석 상태 | 활성화 시 `trackEvent` 헬퍼가 자동 전송하므로 별도 수정 불필요. |
| `window.AFFILIATE` 단일 소스 | 신규 얼라이 카드 추가 시 이 객체에만 URL 추가하면 자동 주입 (라인 1308-1321 패턴 유지). |

---

## 7. C3·C4 구현자 핵심 브리핑

C3 (결과 차별화) 구현 전 필독:
- 보조 타입 표시는 분석 텍스트 내부가 아닌 `result-header` 내부 배지로 ([#2]).
- 적합도 % (`result-match`) 노출 강화 — `font-size:16px; font-weight:800; opacity:1`로 상향.
- 백분위 추가 시 `result-match` 하단에 `result-percentile` 요소 추가, 동일 무게.

C4 (재공유 CTA) 구현 전 필독:
- `/r/{faction}` 랜딩에 재공유 버튼 추가 시 모바일 패딩도 함께 수정 ([#5]).
- 공유 섹션 DOM 순서를 타입 헤더 직후로 이동할 것 ([#6]).
- 공유 레이블 카피를 타입별 동적 생성으로 전환 권고.
